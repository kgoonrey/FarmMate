@model string

<form asp-action="DeleteProductMix" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Delete Product Mix Line" })

    <div class="modal-body form-horizontal">
        Are you want to delete @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>

